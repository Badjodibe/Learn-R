---
title: "manipulation"
format: pdf
editor: visual
---

### 🔸 **Créer une nouvelle variable**

Tu peux créer une nouvelle variable en combinant ou en manipulant des colonnes existantes. Par exemple, créer une nouvelle variable `GDP` (Produit Intérieur Brut) en multipliant le PIB par habitant (`gdpPercap`) par la population (`pop`).

```{r}
gapminder <- gapminder %>%
  mutate(GDP = gdpPercap * pop)

head(gapminder)
```

------------------------------------------------------------------------

### 🔸 **Supprimer une variable**

Tu peux supprimer une colonne avec la fonction `select()` et l'opérateur `-`.

```{r}
gapminder <- gapminder %>%
  select(-GDP)  # Supprimer la colonne GDP

head(gapminder)
```

------------------------------------------------------------------------

### 🔸 **Modifier une variable**

Si tu souhaites modifier une variable, comme ajuster l’espérance de vie (`lifeExp`) en y ajoutant une constante, tu peux le faire en utilisant `mutate()`.

```{r}
gapminder <- gapminder %>%
  mutate(lifeExp = lifeExp + 1)

head(gapminder)
```

------------------------------------------------------------------------

### 🔸 **Renommer une variable**

Si tu veux renommer une colonne, tu peux utiliser `rename()`.

```{r}
gapminder <- gapminder %>%
  rename(Population = pop)

head(gapminder)
```

------------------------------------------------------------------------

### 

### 🔸 **Trier les données**

Tu peux trier les données en fonction de n'importe quelle colonne. Par exemple, trier les pays par PIB par habitant (`gdpPercap`) de manière descendante :

```{r}
gapminder_sorted <- gapminder %>%
  arrange(desc(gdpPercap))

head(gapminder_sorted)
```

------------------------------------------------------------------------

### 🔸 **Sélectionner des variables spécifiques**

Tu peux sélectionner des colonnes spécifiques avec `select()`.

```{r}
gapminder_selected <- gapminder %>%
  select(country, continent, lifeExp)

head(gapminder_selected)
```

------------------------------------------------------------------------

### 🔸 **Regrouper les données et appliquer des fonctions**

Tu peux également regrouper les données par continent et calculer la moyenne de l’espérance de vie pour chaque continent.

```{r}
gapminder_grouped <- gapminder %>%
  group_by(continent) %>%
  summarise(mean_lifeExp = mean(lifeExp))

head(gapminder_grouped)
```

------------------------------------------------------------------------

### 🔸 **Ajouter une colonne avec des conditions (création d’une variable catégorielle)**

Tu peux créer une nouvelle colonne avec des conditions. Par exemple, ajouter une colonne `life_category` qui classifie les pays selon leur espérance de vie en trois catégories :

```{r}
gapminder <- gapminder %>%
  mutate(life_category = case_when(
    lifeExp < 50 ~ "Faible",
    lifeExp >= 50 & lifeExp < 70 ~ "Moyenne",
    lifeExp >= 70 ~ "Élevée"
  ))

head(gapminder)
```
